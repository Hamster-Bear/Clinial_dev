# 前端开发向注释清理清单

> 本文档按模块整理应用中存在的编程/开发向而非用户向的前端注释，供后续统一清理。
> 清理完成后将相关约定归纳至 PROJECT_GUIDE.md。

---

## 一、`modules/statistical_graphics/` — 统计图形子模块

### boxplot.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 29 | subtitle | `"继续保留箱线图 X/Y 核心映射入口"` |
| 33 | app_card_note | `"本轮只统一箱线图外层参数卡和说明块，不调整 X/Y 映射、任务历史恢复或结果数据输出逻辑。"` |
| 51 | helpText | `"当前箱线图模块仅使用 X/Y 核心映射，不包含额外分面、轨道或附加变量控件。"` |
| 64 | subtitle | `"继续保留标题标签、线条点样式与配色设置"` |
| 68 | app_card_note | `"当前统一箱线图样式卡和说明块，不改变标题标签、线条点样式、配色主题或空壳说明页签语义。"` |
| 89 | helpText | `"当前箱线图模块没有额外的显示开关或坐标轴高级设置。"` |
| 118 | helpText | `"当前箱线图模块没有独立的参考线或阈值控件。"` |
| 131 | subtitle | `"继续保留固定画布说明与导出参数链路"` |
| 135 | app_card_note | `"本轮只统一导出卡和说明块，不调整固定 10 x 8 英寸画布、导出格式与 DPI 逻辑。"` |
| 145 | helpText | `"当前箱线图导出默认使用固定画布 10 x 8 英寸。"` |
| 171 | subtitle | `"继续保留动作条与 静态图 / 交互图 / 数据 结果结构"` |
| 175 | app_card_note | `"当前统一箱线图结果卡和说明块，不改变生成图形按钮、下载链路、交互图输出或数据表逻辑。"` |
| 183 | note | `"展示当前箱线图配置生成的静态图结果。"` |
| 192 | note | `"展示当前参数生成的交互式箱线图结果。"` |
| 201 | note | `"继续保留数据表输出，不调整结果数据来源与展示逻辑。"` |

### combo_plot.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 38 | subtitle | `"继续保留主映射、分组分面与组合方式入口"` |
| 42 | app_card_note | `"本轮只统一组合图外层参数卡和说明块，不调整主 X/Y 映射、分组分面、组合方式、多图层选择或任务历史恢复逻辑。"` |
| 58 | help_text | `"当前组合图统一从主 X / Y 映射出发，按已选图层类型叠加或拆分。"` |
| 102 | helpText | `"当前组合图暂无轨道变量；高动态图层参数继续在"图层样式"页签中维护。"` |
| 116 | subtitle | `"继续保留标题说明、动态图层样式与参考占位"` |
| 120 | app_card_note | `"当前统一组合图样式卡和说明块，不改变动态页签参数、自动标题生成、图层叠加方式或参考线占位语义。"` |
| 130 | helpText | `"当前组合图标题仍按图层组合自动生成，本轮优先收口 UI 格局与任务历史恢复，不改绘图语义。"` |
| 140 | helpText | `"组合方式、分组与分面已统一收纳到"数据与变量"页签；当前模块暂不额外暴露统一坐标格式控件。"` |
| 158 | helpText | `"当前组合图模块尚未提供统一的参考线或阈值控件。"` |
| 171 | subtitle | `"继续保留固定画布说明与导出参数链路"` |
| 175 | app_card_note | `"本轮只统一导出卡和说明块，不调整固定 12 x 8 英寸画布、导出格式与 DPI 逻辑。"` |
| 185 | helpText | `"当前组合图导出仍使用固定画布 12 x 8 英寸；本轮不改绘图与导出算法。"` |
| 213 | subtitle | `"继续保留动作条与 静态图 / 交互图 / 数据 结果结构"` |
| 217 | app_card_note | `"当前统一组合图结果卡和说明块，不改变生成图形按钮、下载链路、交互图输出或结果数据表逻辑。"` |
| 225 | note | `"展示当前组合方式和图层配置生成的静态图结果。"` |
| 234 | note | `"展示当前参数生成的交互式组合图结果。"` |
| 243 | note | `"继续保留组合图结果数据表输出，不调整数据整理链路。"` |

### forest_plot.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 1 | # inline | `"# 森林图子模块 - 基于 sample_forest.r 重构（简化版）"` |
| 40 | subtitle | `"继续保留双模式分析入口、列映射与表格列配置"` |
| 44 | app_card_note | `"本轮只统一森林图外层参数卡和说明块，不调整 precalculated/raw_data 双模式、列映射恢复或表格列选择逻辑。"` |
| 140 | subtitle | `"继续保留标题、坐标、图层配色与参考线配置"` |
| 144 | app_card_note | `"当前统一森林图外层样式卡和说明块，不改变标题脚注、配色模式、坐标范围、表格宽度比例和参考线语义。"` |
| 279 | subtitle | `"继续保留尺寸设置、画布比例与导出参数链路"` |
| 283 | app_card_note | `"本轮只统一导出卡和说明块，不调整导出尺寸、宽高比例、DPI 与文件格式处理逻辑。"` |
| 324 | subtitle | `"继续保留动作条与 静态图 / 交互图 / 数据 结果结构"` |
| 328 | app_card_note | `"当前统一森林图结果卡和说明块，不改变生成图形按钮、导出链路、数据预览和统计报告输出逻辑。"` |
| 336 | note | `"展示当前森林图配置生成的主图与表格组合结果。"` |
| 345 | note | `"当前森林图模块尚未提供独立交互图结果；本轮仅统一结果区结构，不改绘图实现。"` |
| 357 | note | `"继续保留数据预览和统计报告双页签，不调整结果 schema、分析解释或报告链路。"` |
| 1234 | # inline | `"# 注意：在 raw_data 模式下，P_Value_Str 已经是格式化好的字符串，不需要再次格式化"` |
| 1241 | # inline | `"# 先暂时保留 NA/空值处理逻辑"` |

### spider_plot.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 27 | subtitle | `"继续保留核心映射、分组与分面变量链路"` |
| 31 | app_card_note | `"本轮只统一蜘蛛图外层参数卡和说明块，不调整受试者/时间/变化值映射、颜色分组、分面变量或任务历史恢复逻辑。"` |
| 71 | subtitle | `"继续保留标题、坐标、图层样式与 RECIST 阈值控制"` |
| 75 | app_card_note | `"当前统一蜘蛛图样式卡和说明块，不改变时间单位换算、Y 轴标签格式、点层/末次标签、补基线原点和 RECIST 参考线语义。"` |
| 144 | helpText | `"这里只控制 Y 轴标签显示格式，不会对原始变化值重新换算。"` |
| 196 | subtitle | `"继续保留尺寸模式、画布同步与导出参数链路"` |
| 200 | app_card_note | `"本轮只统一导出卡和说明块，不调整前端尺寸同步、PX/英寸换算、导出格式与 committed 静态图尺寸逻辑。"` |
| 268 | subtitle | `"继续保留动作条与 静态图 / 交互图 / 数据 结果结构"` |
| 272 | app_card_note | `"当前统一蜘蛛图结果卡和说明块，不改变生成图形按钮、下载链路、数据表输出或 committed 参数快照逻辑。"` |
| 280 | note | `"展示当前 committed 参数生成的蜘蛛图静态结果。"` |
| 289 | note | `"展示当前参数生成的交互式蜘蛛图结果。"` |
| 298 | note | `"继续保留蜘蛛图结果数据表输出，不调整数据整理链路。"` |

### survival_analysis.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 766 | subtitle | `"继续保留动作条与 静态图 / 交互图 / 数据 结果结构"` |
| 770 | app_card_note | `"当前统一结果卡片与说明块，不改变生成图形按钮、下载链路、结果页签或统计报告输出逻辑。"` |
| 778 | note | `"展示当前提交态参数生成的静态生存曲线与风险表组合结果。"` |
| 787 | note | `"展示当前提交态参数对应的 Plotly 交互式生存曲线结果。"` |
| 796 | note | `"继续保留数据表与统计报告双页签，不调整汇总数据或报告解释链路。"` |
| 842 | subtitle | `"继续保留核心映射、分组分面与时间范围配置链路"` |
| 846 | app_card_note | `"本轮只统一顶层参数卡视觉与说明块，不调整时间/状态变量选择、分层参考组、分面值和时间轴控制逻辑。"` |
| 858 | subtitle | `"继续保留显示坐标、标题文字、图层样式与阈值设置"` |
| 862 | app_card_note | `"当前统一顶层功能卡与说明块，不改变中位生存辅助线、统计摘要、图例文字与风险表文字的原有业务语义。"` |
| 880 | subtitle | `"继续保留尺寸模式、导出格式与分层标签输出链路"` |
| 884 | app_card_note | `"本轮只统一顶层导出卡与说明块，不调整画布尺寸同步、导出 DPI、格式选择或分层标签恢复逻辑。"` |
| 1071 | # inline | `"# 注意：这里我们不再依赖 graphics_state$km_facet_values，而是直接看当前是否有有效选项"` |

### swimmer_plot.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 29 | subtitle | `"继续保留核心映射、事件映射与轨道变量链路"` |
| 33 | app_card_note | `"本轮只统一泳道图外层参数卡和说明块，不调整时间模式选择、事件映射行恢复、轨道变量和排序逻辑。"` |
| 72 | help_text | `"泳道图当前没有独立分面变量；事件映射和轨道变量统一收纳在本页签。"` |
| 87 | helpText | `"默认不自动填入轨道变量；仅当你明确选择后，才在下方轨道区和分组轨道数据中显示。"` |
| 101 | helpText | `"排序基于每位受试者汇总后的主泳道长度或绘图终点位置；若使用日期型起止时间，这里的"泳道终点"指换算后的随访终点位置，而不是原始日历日期。"` |
| 104 | helpText | `"这里设置的是新选轨道的默认展示方式；下方"分组轨道展示方式"可对每条轨道分别改成颜色填充或文本填充。"` |
| 119 | subtitle | `"继续保留标题、坐标、事件图例与轨道区版式控制"` |
| 123 | app_card_note | `"当前统一泳道图样式卡和说明块，不改变事件样式、主图颜色映射、缺失值文本、下方轨道区占比和参考页签占位语义。"` |
| 224 | help_text | `"这里控制事件图例整体层级；每个事件变量组自己的图例标题在"事件映射"中逐组设置。"` |
| 264 | helpText | `"这里控制下方分组轨道区的显示方式与总图例标题，不影响泳道主图的颜色分组图例。"` |
| 283 | helpText | `""空值显示方式"影响轨道和数据表中的缺失文本；"下方轨道区占比"和"全局字号"影响主图与下方轨道区的版式比例。"` |
| 295-296 | helpText | `"泳道图当前没有独立的参考线或阈值控件。"` + `"统一保留该页签位，后续如补充业务阈值线，可直接按同一结构接入。"` |
| 309 | subtitle | `"继续保留尺寸模式、画布同步与导出参数链路"` |
| 313 | app_card_note | `"本轮只统一导出卡和说明块，不调整前端尺寸同步、PX/英寸换算、导出格式与 committed 静态图尺寸逻辑。"` |
| 348 | helpText | `"默认按 PX/英寸换算同步导出尺寸，并保持前端静态图与导出比例一致。"` |
| 383 | subtitle | `"继续保留动作条与 静态图 / 交互图 / 数据 结果结构"` |
| 387 | app_card_note | `"当前统一泳道图结果卡和说明块，不改变生成图形按钮、下载链路、事件数据/轨道数据输出或 committed 参数快照逻辑。"` |
| 395 | note | `"展示当前 committed 参数生成的主泳道图、事件层和下方轨道区结果。"` |
| 404 | note | `"展示当前参数生成的交互式泳道图结果。"` |
| 413 | note | `"继续保留泳道数据、事件数据和分组轨道数据三页签，不调整数据整理或导出链路。"` |

### waterfall_plot.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 33 | subtitle | `"继续保留核心映射、分组轨道与排序链路"` |
| 37 | app_card_note | `"本轮只统一瀑布图外层参数卡和说明块，不调整受试者映射、变化值选择、排序方向、轨道变量或任务历史恢复逻辑。"` |
| 64 | help_text | `"当前瀑布图没有独立分面变量；轨道变量和附加符号映射统一收纳在此页签。"` |
| 70 | helpText | `"默认不自动填入轨道变量；仅当你明确选择后，才在下方轨道区和分组轨道数据中显示。"` |
| 86 | subtitle | `"继续保留标题、坐标、图层样式与 RECIST 阈值控制"` |
| 90 | app_card_note | `"当前统一瀑布图样式卡和说明块，不改变百分比标签格式、符号文本映射、轨道区版式和 RECIST 参考线语义。"` |
| 137 | help_text | `"这里只控制 Y 轴标签的显示格式，不会对原始变化值重新换算。"` |
| 227 | helpText | `"这里控制下方轨道区的颜色、文本显示和缺失值文本，不影响主图柱体颜色与符号映射。"` |
| 260 | subtitle | `"继续保留尺寸模式、画布同步与导出参数链路"` |
| 264 | app_card_note | `"本轮只统一导出卡和说明块，不调整前端尺寸同步、PX/英寸换算、导出格式与 DPI 逻辑。"` |
| 299 | helpText | `"默认按 PX/英寸换算同步导出尺寸，并保持前端静态图与导出比例一致。"` |
| 334 | subtitle | `"继续保留动作条与 静态图 / 交互图 / 数据 结果结构"` |
| 338 | app_card_note | `"当前统一瀑布图结果卡和说明块，不改变生成图形按钮、下载链路、数据表输出或 committed 参数快照逻辑。"` |
| 346 | note | `"展示当前 committed 参数生成的瀑布图主图和下方轨道区结果。"` |
| 355 | note | `"展示当前参数生成的交互式瀑布图结果。"` |
| 364 | note | `"继续保留瀑布数据与分组轨道数据双页签，不调整数据整理或导出链路。"` |

### correlation_matrix.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 40 | help_text | `"相关性矩阵显示变量间的相关系数"` |
| 49 | helpText | `"当前相关性矩阵模块没有独立的分组、分面或轨道变量控件。"` |
| 79 | helpText | `"当前相关性矩阵模块没有额外的显示开关或坐标轴高级设置。"` |
| 104 | helpText | `"当前相关性矩阵模块没有独立的参考线或阈值控件。"` |
| 123 | helpText | `"当前相关性矩阵导出默认使用固定画布 10 x 8 英寸。"` |

### heatmap.R
(需额外检查)

---

## 二、`modules/statistical_analysis/` — 统计分析子模块

### statistical_analysis.R (根入口)
| 行号 | 类型 | 内容 |
|------|------|------|
| 62 | subtitle | `"先统一设置当前统计分析总入口使用的数据过滤条件"` |
| 68 | app_card_note | `"该筛选作用于当前统计分析总入口下的所有方法；本轮只统一入口卡片视觉，不改筛选逻辑。"` |
| 82 | subtitle | `"在总入口内切换描述性统计、回归模型与组间比较方法"` |
| 86 | app_card_note | `"当前总入口已接入公共卡片壳，但各统计子模块内部参数 UI 与分析逻辑保持原样。"` |
| 114 | subtitle | `"按当前方法动态加载变量映射、模型参数与执行入口"` |
| 118 | app_card_note | `"这里只统一卡片标题、说明块与边框留白，不调整各方法参数项的业务语义。"` |
| 139 | subtitle | `"统一结果容器、空状态与导出说明，但保留原有结果结构与业务链路"` |
| 143 | app_card_note | `"结果区继续保留统计表格、统计报告与可复现代码三段结构；本轮进一步统一 tab 内结果容器、空状态与导出说明块。"` |
| 149 | note | `"展示当前分析方法生成的主结果表；未运行分析时显示统一空状态提示。"` |
| 158 | note | `"汇总当前分析方法的关键解释、结果阅读提示与上下文说明。"` |
| 167 | note | `"提供当前分析参数对应的 R 代码草稿，便于在本地或报告流程中复现。"` |
| 176 | note | `"继续沿用当前格式、报告开关、标题与脚注配置，不调整导出文件生成逻辑。"` |

### anova.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 16 | app_card_note | `"ANOVA 参数区已接入公共壳分组样式；本轮只统一说明块与参数分区，不调整方差分析公式与结果输出逻辑。"` |
| 19 | app_card_note | `"选择连续型响应变量，作为方差分析的因变量。"` |
| 24 | app_card_note | `"可同时选择一个或多个因子变量，后续仍按原有交互项公式拼接方式进行方差分析。"` |

### chisq.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 15 | app_card_note | `"卡方检验参数区已接入公共壳分组样式；本轮只统一说明块与参数分区，不调整列联表构造与检验逻辑。"` |
| 18 | app_card_note | `"选择两列分类型变量，继续按原有列联表与卡方检验路径计算统计量、自由度和 P 值。"` |

### cox.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 24 | app_card_note | `"Cox 回归参数区已接入公共壳分组样式；本轮只统一参数分区与说明块，不改变建模、交互检验或结果展示逻辑。"` |
| 27 | app_card_note | `"先定义生存时间、事件状态，再按需设置亚组、分面与模型内分层因素。"` |
| 57 | app_card_note | `"继续保留总计列设置与事件值映射，用于控制列展示与删失/事件判定。"` |
| 63 | app_card_note | `"协变量选择、分类变量参考组与后续模型公式保持原有处理方式。"` |

### desc.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 25 | app_card_note | `"描述性统计参数区已接入公共壳分组样式；本轮只统一说明块与参数分区，不调整统计汇总逻辑。"` |
| 28 | app_card_note | `"先选择分析变量，再按需指定列分组、行分组和受试者唯一标识变量。"` |
| 36 | app_card_note | `"继续保留总计列配置、固定小数位数与自动小数位数策略。"` |

### linear.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 24 | app_card_note | `"Linear 回归参数区已接入公共壳分组样式；本轮只统一参数分区与说明块，不改变建模、总计列或结果展示逻辑。"` |
| 27 | app_card_note | `"先定义连续型响应变量，再按需设置亚组、分面与模型内分层因素。"` |
| 53 | app_card_note | `"继续保留自定义总计列配置，用于控制线性回归结果中的列展示。"` |
| 58 | app_card_note | `"预测变量选择、分类变量参考组与后续模型公式保持原有处理方式。"` |

### logistic.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 24 | app_card_note | `"Logistic 回归参数区已接入公共壳分组样式；本轮只统一参数分区与说明块，不改变事件映射、建模或结果展示逻辑。"` |
| 27 | app_card_note | `"先定义二分类响应变量，再按需设置亚组、分面与模型内分层因素。"` |
| 53 | app_card_note | `"继续保留总计列设置与事件值映射，用于控制列展示与二分类事件判定。"` |
| 59 | app_card_note | `"预测变量选择、分类变量参考组与后续模型公式保持原有处理方式。"` |

---

## 三、`modules/tables/` — 表格子模块

### tables.R (根入口)
| 行号 | 类型 | 内容 |
|------|------|------|
| 34 | subtitle | `"继续保留表格类型切换与动态参数链路，只统一入口卡片视觉"` |
| 40 | app_card_note | `"全局筛选卡已作为公共模块独立收口；这里统一参数入口卡、动态参数容器与生成按钮区。"` |
| 56 | app_card_note | `"根据当前表格类型动态渲染参数 UI，不改变已有参数计算、可用性控制和生成逻辑。"` |
| 61 | app_card_note | `"在当前参数可用时生成结果；按钮启停逻辑保持原有 server 校验。"` |
| 78 | subtitle | `"继续保留表格结果与 R 代码结构，只统一结果卡和导出说明块"` |
| 84 | app_card_note | `"结果区继续保留"表格结果 / R代码"结构，不改变输出对象、渲染方式或导出逻辑。"` |
| 90 | note | `"展示当前所选表格类型生成的结果对象；渲染方式继续由类型分支决定。"` |
| 99 | note | `"展示当前表格参数对应的代码草稿，便于复现或写入报告。"` |
| 107 | note | `"继续沿用现有格式选择、文件名前缀和导出按钮，不调整底层导出实现。"` |

### t_dm.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 43 | helpText | `"注：变量类型将根据数据类型自动判断，数值型为连续变量，字符/因子型为分类变量"` |
| 264 | # inline | `"# 后续可通过 modify_header 调整"` |

### t_ae_soc_pt.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 112 | helpText | `"注：表格将按一级分类分层，统计各分组和总体的事件数与受试者数。"` |

### ae_sidebyside.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 117 | # inline | `"# 为了简化，我们暂时只处理第一个组，或者忽略分组变量（展示总体）。"` |
| 120 | # inline | `"# 但为了严谨，我们计算 N_TOTAL 时需要注意。"` |
| 230 | # inline | `"# 注意：如果某个 Term 没有最高 Grade，可能需要调整排序逻辑"` |

### listing_general.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 56 | # inline | `"# 下载按钮 (注意：需要在 server 端绑定 downloadHandler)"` |
| 78 | # inline | `"# 优化：先排序，再格式化为字符，确保数值列按大小排序而非字典序"` |
| 119 | # inline | `"# 优化：先排序，再格式化为字符，确保数值列按大小排序而非字典序"` |

---

## 四、`modules/common/` — 公共层

### data_filter.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 98 | subtitle | `"作为全局公共筛选工作台复用；默认折叠，不干扰主流程"` |
| 105 | app_card_note | `"当前统一可折叠工作台卡片、按钮区和筛选条件容器，不改变筛选语义、动态控件生成与应用时机。"` |
| 144 | app_card_note | `"按变量类型动态生成数值、分类、日期或文本条件；所选条件在点击"应用筛选"后统一生效。"` |

### common_ui_shell.R (modules/common/ui_shell.R)
| 行号 | 类型 | 内容 |
|------|------|------|
| (无 dev 向注释——`app_card_note`/`subtitle` 的调用方，本身是通用组件) | — | — |

### graphics_common.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 1060 | # inline | `"# "default" 不做任何操作，保留原主题默认样式"` |

### analysis_shared.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 1070 | # inline | `"# UI 生成器 (重构版本)"` |

---

## 五、`modules/account_access/` — 账号与权限

### permission_manager.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 147 | subtitle | `"权限数据加载异常"` |
| 151 | app_card_note | `"当前无法加载可管理空间或已授权空间数据。..."` |
| 171 | app_card_note | `"当前账号不是这些数据空间的负责人，因此这里聚合展示的是你被授予的访问权限信息，而不是成员管理表单。"` |
| 182 | app_card_panel | `"若后续获得负责人权限或创建新的数据空间，当前页会自动切换为协作工作台布局。"` |
| 204 | app_card_note | `"当前账号名下还没有可管理的数据空间，因此权限管理区暂时不显示协作设置表单。后续创建或获得可管理空间后，这里会自动出现成员授权、撤销和负责人迁移能力。"` |
| 219 | app_card_note | `"当前页将可管理空间概览、邮箱授权、负责人迁移与成员预览聚合展示，减少上下跳转。"` |
| 275 | app_card_note | `"请先登录后查看已授权空间并管理数据空间协作权限。"` |
| 282 | paste0 | `"当前页只负责权限相关数据。若权限数据加载失败，不应影响"用户信息"页继续使用。"` |
| 288 | paste0 | `"当前页只负责数据空间协作权限。新增协作者、撤销协作和迁移负责人都通过邮箱完成，不展示库内用户选择器。"` |
| 290 | app_card_note | (同 282/288 拼接结果) |
| 302 | subtitle | `"权限区渲染失败"` |
| 306 | app_card_note | `"权限区渲染失败，请稍后重试。"` |
| 331 | app_card_note | `"当前管理上下文加载失败"` |

### user_profile.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 55 | 说明文本 | `"当前邮箱尚未验证，可在本页直接发送验证码并完成验证。邮箱验证不会阻断登录，但建议尽快完成，以便后续协作授权与邮箱换绑流程正常使用。"` |
| 67 | 说明文本 | `"建议先完成邮箱验证，避免后续协作与换绑流程受限。"` |
| 80 | app_card_note | (build_profile_notice 动态生成) |
| 98 | app_card_note | `"当前页将账号安全相关操作集中到同一张功能卡片中，减少纵向滚动和重复说明。"` |
| 174 | app_card_note | `"用户信息区渲染失败，请稍后重试。"` |

---

## 六、`modules/` 根级模块

### statistical_graphics.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 37 | subtitle | `"继续保留图形子模块切换链路，只统一入口卡片视觉与说明块"` |
| 41 | app_card_note | `"全局筛选卡和任务历史卡已作为公共模块独立收口；这里仅保留图形类型选择入口。"` |
| 62 | help_text | `"当前已支持将图形参数、界面状态与模块类型保存为任务历史；按用户隔离，workspace 为空时保存为个人任务。"` |
| 160 | subtitle | `"按当前图形类型和参数生成代码草稿"` |
| 166 | app_card_note | `"继续保留原有代码生成逻辑，只统一代码容器与说明块。"` |
| 169 | note | `"根据当前图形模块和参数状态生成 R 代码草稿，便于本地复现或文档整理。"` |

### statistical_analysis.R — 见第二章

### tables.R — 见第三章

### task_history.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 35 | subtitle | `"按用户与模块类型保存、加载、刷新和删除任务历史；默认折叠"` |
| 42 | app_card_note | `"当前统一任务历史工作台卡片、操作按钮区和说明块，不改变任务历史持久化、加载回填与删除逻辑。"` |
| 66 | app_card_note | `"表格按最近更新时间展示当前模块下可加载的任务历史；选择后可同步回填任务名称和备注。"` |

### data_preparation.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 98 | subtitle | `"统一承接本地上传与数据库数据集加载，不改变原有加载能力"` |
| 115 | app_card_note | (批量上传说明) |
| 139 | app_card_note | `"从已授权的数据空间、文件夹和数据集中继续分析现有数据，不改动数据库侧原有结构。"` |
| 156 | subtitle | `"在同一块中完成筛选变量选择、显示列控制和筛选结果应用"` |
| 223 | subtitle | `"按变量类型动态生成筛选控件，并在增减筛选变量时尽量保留已选值"` |
| 246 | subtitle | `"查看当前筛选结果与表格渲染状态"` |
| 267 | subtitle | `"集中查看变量类型、Label，并批量调整元数据"` |
| 311 | # inline | `"# 缓存列定义函数 - 使用memoise优化"` |

### exploratory_analysis.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 14 | subtitle | `"继续保留可用变量浏览与类型提示，只统一入口卡片视觉"` |
| 18 | app_card_note | `"当前统一变量托盘卡片、说明块与滚动容器，不调整变量类型判定或输出内容。"` |
| 29 | subtitle | `"继续保留图形类型切换、变量映射与标题设置链路"` |
| 33 | app_card_note | `"本轮只统一控制器入口卡、参数分区与说明块，不改变 tooltip、动态变量过滤与重置行为。"` |
| 46 | app_card_note | `"继续通过动态 UI 输出 X/Y/颜色/分面变量选择；图形类型切换后保持原有变量过滤规则。"` |
| 68 | app_card_note | `"继续保留图形标题、副标题和坐标轴标签自定义输入，不改变默认回退文案逻辑。"` |
| 92 | subtitle | `"继续保留 Plotly 结果输出与分页提示链路"` |
| 96 | app_card_note | `"当前统一结果卡与说明块，不调整 Plotly 渲染、错误占位图或分页提示逻辑。"` |
| 99 | note | `"展示当前变量映射生成的交互式图形，并继续保留分页说明与异常提示输出。"` |

### auth_manager.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 82 | subtitle | `"统一认证入口"` |
| 86 | app_card_note | `"请先登录后进入工作台。当前支持用户名或邮箱登录，邮箱验证、换绑与协作能力会继续围绕邮箱身份扩展。"` |
| 91 | subtitle | `"输入账号信息进入工作台"` |
| 122 | subtitle | `"创建基础账号"` |
| 126 | app_card_note | `"注册后可由管理员开放数据库管理能力，并可通过邮箱加入协作数据空间；邮箱验证改为登录后在用户信息中自行完成。"` |
| 131 | subtitle | `"填写基础注册信息"` |
| 139 | textInput placeholder | `"后续可用于协作授权与找回流程"` |
| 163 | subtitle | `"独立找回流程"` |
| 167 | app_card_note | `"通过注册邮箱申请重置验证码，再用验证码设置新密码。"` |
| 172 | subtitle | `"使用验证码重置密码"` |

### database_manager.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 73 | subtitle | `"围绕数据空间、目录、数据集和结构总览统一完成整理与导入"` |
| 77 | app_card_note | (空间管理说明) |
| 372 | subtitle | `"当前账号尚未开放数据空间管理能力"` |
| 442 | subtitle | `"选择、创建或删除当前数据空间"` |
| 453 | app_card_note | `"创建后会自动把当前登录用户设为负责人；删除数据空间仅允许负责人或管理员执行。"` |
| 465 | subtitle | `"围绕当前数据空间维护目录结构"` |
| 476 | app_card_note | `"目录只在当前数据空间下生效；根目录下的数据集会直接展示在空间级结构中。"` |
| 488 | subtitle | `"管理当前目录下的数据集选择与删除"` |
| 499 | app_card_note | `"上传时可以覆盖显示名称；删除仅影响当前数据空间内选中的数据集。"` |
| 515 | subtitle | `"适合逐个整理数据集名称与目录归属"` |
| 527 | app_card_note | `"适合逐个整理数据集名称与目录归属。"` |
| 536 | subtitle | `"适合初始化当前目录下的一批数据集与服务器目录导入"` |
| 548 | app_card_note | `"适合初始化当前目录下的一批数据集。服务器目录导入仅对系统管理员开放。"` |

### admin_manager.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 168 | subtitle | `"集中处理系统级账号状态、数据空间权限与协作排障入口"` |
| 422-426 | app_card_note | (管理员权限说明) |
| 443 | subtitle | `"摘要优先查看当前管理员与账号总体态势"` |
| 452 | subtitle | `"聚焦停用账号、未设置邮箱与待领取邀请等风险"` |
| 461 | subtitle | `"快速核对数据库与管理员预置环境变量"` |
| 478 | subtitle | `"按元信息统一筛查账号状态、数据空间功能与协作概况"` |
| 482 | app_card_note | `"仅展示所有已注册账号的元信息、数据空间功能状态和协作摘要，便于系统管理员统一排查与筛查；不展示其他用户数据空间中的实际数据内容。"` |
| 491 | subtitle | `"管理员可向测试邮箱发送探针邮件验证投递链路"` |
| 495 | app_card_note | `"仅用于验证当前邮件投递配置是否可达。建议先使用管理员自己的测试邮箱或预发收件箱验收。"` |
| 525 | subtitle | `"从上方账号总览联动目标账号后执行启停与开关"` |
| 530 | app_card_note | `"先在上方"所有注册账号总览"中选中目标账号，再根据状态卡片与操作影响预览决定启用、停用或开关数据空间功能。"` |
| 544 | subtitle | `"合并处理我名下空间的负责人调整与协作授权"` |
| 549 | app_card_note | `"当前卡片统一处理我名下数据空间的负责人迁移、协作授权与待领取邀请跟进，不展示库内用户选择器。"` |
| 589 | subtitle | `"仅展示当前管理员可管理的数据空间元信息"` |
| 594 | app_card_note | `"仅展示当前管理员自己名下可管理的数据空间元信息，用于快速判断共享状态、待领取邀请和负责人迁移风险。"` |
| 866 | app_card_note | `"请先在"所有注册账号总览"中选中一个账号。"` |
| 906 | app_card_note | `"当前管理员名下暂无可管理数据空间。"` |

---

## 七、`statistical_graphics_ui/` — 图形公共 UI 组件

### common_ui_shell.R
| 行号 | 类型 | 内容 |
|------|------|------|
| 48 | helpText | `"默认按 PX/英寸换算同步导出尺寸，并保持前端静态图与导出比例一致。"` |

---

## 八、模式总结

上表中标注的注释可归为以下模式：

| 模式 | 说明 | 示例 |
|------|------|------|
| **"本轮只统一...不调整..."** | 本轮 UI 重构的范围说明，标注哪些部分刻意不改 | `"本轮只统一箱线图外层参数卡和说明块，不调整 X/Y 映射..."` |
| **"继续保留...只统一..."** | 保持原有业务链路不变，仅统一视觉 | `"继续保留图形子模块切换链路，只统一入口卡片视觉与说明块"` |
| **"当前统一...不改变..."** | 当前正在进行的统一工作说明 | `"当前统一结果卡与说明块，不调整 Plotly 渲染..."` |
| **"继续沿用...不调整..."** | 沿用已有的实现，不去改动 | `"继续沿用现有格式选择、文件名前缀和导出按钮，不调整底层导出实现。"` |
| **"这里只控制...不影响..."** | helpText 中使用，解释控件作用边界 | `"这里只控制 Y 轴标签显示格式，不会对原始变化值重新换算。"` |
| **"没有独立...控件"** | helpText 说明当前模块不具备某项功能 | `"当前箱线图模块没有独立的参考线或阈值控件。"` |
| **# inline 注释** | 行内代码注释，标注待办、注意、优化说明 | `"# 注意：在 raw_data 模式下，P_Value_Str 已经是格式化好的字符串"` |

统计：
- `app_card_note()` 调用：约 80+ 处
- `subtitle` 参数：约 50+ 处
- `helpText()` / `help_text`：约 30+ 处
- `note` 参数：约 30+ 处
- `# inline` 注释：约 15 处

GRAPHICS_RESULT_COPY <- list(
  boxplot = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、数据表和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前箱线图配置生成的静态图结果。"
    ),
    interactive_plot = list(
      note = "展示当前参数生成的交互式箱线图结果。"
    ),
    data_tab = list(
      note = "查看当前箱线图对应的结果数据表。"
    )
  ),
  survival = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、数据表、统计报告和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前提交态参数生成的静态生存曲线与风险表组合结果。"
    ),
    interactive_plot = list(
      note = "展示当前提交态参数对应的 Plotly 交互式生存曲线结果。"
    ),
    data_tab = list(
      note = "查看数据表和统计报告。"
    )
  ),
  forest = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、数据预览、统计报告和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前森林图配置生成的主图与表格组合结果。"
    ),
    data_tab = list(
      note = "查看数据预览和统计报告。"
    )
  ),
  heatmap = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、数据表和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前热图配置生成的静态图结果。"
    ),
    interactive_plot = list(
      note = "展示当前参数生成的交互式热图结果。"
    ),
    data_tab = list(
      note = "查看当前热图对应的矩阵结果数据。"
    )
  ),
  correlation = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、数据表和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前相关性矩阵配置生成的静态图结果。"
    ),
    interactive_plot = list(
      note = "展示当前参数生成的交互式相关性矩阵结果。"
    ),
    data_tab = list(
      note = "查看当前相关矩阵对应的结果数据表。"
    )
  ),
  combo = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、结果数据表和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前组合方式和图层配置生成的静态图结果。"
    ),
    interactive_plot = list(
      note = "展示当前参数生成的交互式组合图结果。"
    ),
    data_tab = list(
      note = "查看当前组合图对应的结果数据表。"
    )
  ),
  waterfall = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、结果数据表和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前 committed 参数生成的瀑布图主图和下方轨道区结果。"
    ),
    interactive_plot = list(
      note = "展示当前参数生成的交互式瀑布图结果。"
    ),
    data_tab = list(
      note = "查看瀑布数据和分组轨道数据。"
    )
  ),
  swimmer = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、泳道相关结果数据和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前 committed 参数生成的主泳道图、事件层和下方轨道区结果。"
    ),
    interactive_plot = list(
      note = "展示当前参数生成的交互式泳道图结果。"
    ),
    data_tab = list(
      note = "查看泳道数据、事件数据和分组轨道数据。"
    )
  ),
  spider = list(
    result_card = list(
      subtitle = "查看结果并导出输出",
      note = "结果区提供生成图形、下载、交互图、结果数据表和可复现代码。"
    ),
    static_plot = list(
      note = "展示当前 committed 参数生成的蜘蛛图静态结果。"
    ),
    interactive_plot = list(
      note = "展示当前参数生成的交互式蜘蛛图结果。"
    ),
    data_tab = list(
      note = "查看当前蜘蛛图对应的结果数据表。"
    )
  )
)

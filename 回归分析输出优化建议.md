# 回归分析输出优化建议

## 文档目标
本文档旨在针对AutoTFL统计分析模块中回归分析输出存在的**前后端不一致**与**层级缩进失效**问题，提出**最小化改动**的修复方案。所有建议均以**保持现有表格结构**为前提，不改变用户已熟悉的前后端显示布局。

## 问题现状

基于代码审计与测试验证，当前回归分析输出在基础场景下工作正常，但在复杂分层分组组合中暴露出以下问题：

### 1. 交互作用P值行缩进不当
- **现象**：在单亚组场景（无列分组）中，交互作用P值行（`*P for interaction*`）被添加了与变量子水平相同的缩进（4个`\u00A0`）。
- **影响**：交互作用P值应作为变量块的**脚注行**，其缩进层级应与变量标题行一致，而非与水平行同级。
- **示例输出**：
  ```
  预测变量: "    *P for interaction*"  （4个\u00A0缩进）
  ```

### 2. 亚组差异P值列显示逻辑混乱
- **现象**：在“亚组+列分组”场景下，`亚组差异P值`列会在多个变量行重复显示（如age行和sex行均有值）。
- **影响**：交互作用P值本应仅针对当前变量，重复显示导致含义模糊，且与单亚组场景（交互作用P值单独成行）的输出形式不一致。
- **示例输出**：
  ```
  age行 亚组差异P值: "0.541"
  sex行 亚组差异P值: "0.273"
  ```

### 3. 导出流程潜在风险
- **潜在点**：`extract_table_dataframe` 和 `build_sci_flextable` 中的清理函数可能意外移除或转换Unicode缩进字符（`\u00A0`），导致导出文档与前端显示不一致。

## 设计原则

1. **保持结构稳定**：不改变现有表格的列结构、行序、父级标题行插入等核心布局。
2. **最小化改动**：仅修复缩进层级与显示逻辑，不重构核心算法。
3. **前后端一致**：确保HTML渲染、数据提取、文档导出三者的显示完全一致。

## 具体修复建议

### 修复1：调整交互作用P值行的缩进层级

**修改文件**：`modules/common/analysis_shared.R`

**目标**：将交互作用P值行作为变量块的脚注行，减少缩进层级。

**当前代码（第562行）**：
```r
预测变量 = paste0("\U00A0\U00A0\U00A0\U00A0*P for interaction*")
```

**建议修改**：
```r
预测变量 = paste0("\U00A0\U00A0*P for interaction*")  # 改为2个\u00A0缩进
```
或
```r
预测变量 = "*P for interaction*"  # 无缩进，作为独立脚注行
```

**选择建议**：采用**2个`\u00A0`缩进**，既区别于水平行（4个缩进），又保持一定的视觉层级。

### 修复2：修正亚组差异P值列的显示逻辑

**修改文件**：`modules/common/analysis_shared.R`

**目标**：确保每变量仅在其**标题行**（`.__row_order == -1L`）显示交互作用P值。

**当前逻辑（第545-551行）**：
```r
for (i in seq_len(nrow(final_df))) {
  if (final_df$.__row_order[i] == -1L && as.character(final_df$亚组[i]) == p_level) {
    # 设置亚组差异P值
  }
}
```

**问题**：对于分类变量，标题行可能不存在（`.__row_order == -1L`的行可能缺失），导致逻辑跳过。

**建议修改**：
```r
# 为每个变量找到其对应的标题行或首个水平行
for (v_raw in unique(final_df$预测变量原始)) {
  if (!nzchar(v_raw)) next
  # 查找该变量的标题行（.__row_order == -1L）
  title_idx <- which(final_df$预测变量原始 == v_raw & final_df$.__row_order == -1L)
  if (length(title_idx) == 0) {
    # 如果标题行不存在，找该变量的第一行
    var_rows <- which(final_df$预测变量原始 == v_raw)
    if (length(var_rows) > 0) title_idx <- var_rows[1]
  }
  if (length(title_idx) > 0 && as.character(final_df$亚组[title_idx[1]]) == p_level) {
    facet_tag <- if ("列分组" %in% names(final_df)) as.character(final_df$列分组[title_idx[1]]) else "__ALL__"
    pval <- get_int_p_fn(int_p_map, v_raw, p_level, facet_tag)
    final_df$亚组差异P值[title_idx[1]] <- pval
  }
}
```

### 修复3：增强导出流程的缩进保留

**修改文件**：`modules/common/table_export.R`

**目标**：确保Unicode缩进字符在数据提取和清理过程中不被意外移除。

**检查点1**：`clean_label` 函数（第57-64行）
```r
clean_label <- function(x) {
  txt <- gsub("(?i)<br\\s*/?>", "\n", x, perl = TRUE)
  txt <- gsub("<[^>]+>", "", txt)
  txt <- gsub("&nbsp;", " ", txt, fixed = TRUE)  # 注意：将&nbsp;转为空格
  txt <- gsub("[ \t]*\n[ \t]*", "\n", txt, perl = TRUE)
  txt <- trimws(txt)  # 注意：可能移除行首的\u00A0
  ifelse(!nzchar(txt), NA_character_, txt)
}
```

**检查点2**：`clean_gt_label` 函数（第226-235行）
```r
clean_gt_label <- function(x) {
  txt <- gsub("(?i)<br\\s*/?>", "\n", as.character(x), perl = TRUE)
  txt <- gsub("\\s{2,}\\n", "\n", txt, perl = TRUE)
  txt <- gsub("<[^>]+>", "", txt)
  txt <- gsub("&nbsp;", " ", txt, fixed = TRUE)  # 注意
  txt <- gsub("[ \t]*\n[ \t]*", "\n", txt, perl = TRUE)
  txt <- gsub("\\s*\\(N\\s*=\\s*([^\\)]+)\\)", "\n(N = \\1)", txt, perl = TRUE)
  txt <- gsub("\n{2,}", "\n", txt, perl = TRUE)
  trimws(txt)  # 注意：可能移除行首的\u00A0
}
```

**建议修改**：
1. 在 `trimws` 调用前，先将 `\u00A0` 替换为占位符，trim后再恢复。
2. 或使用自定义的 `trim_keep_nbsp` 函数，仅移除普通空格，保留 `\u00A0`。

**示例实现**：
```r
trim_keep_nbsp <- function(x) {
  # 将 \u00A0 替换为临时标记
  temp <- gsub("\u00A0", "\u0001", x, fixed = TRUE)
  # 移除首尾普通空格
  temp <- gsub("^[ \t\r\n]+|[ \t\r\n]+$", "", temp)
  # 恢复 \u00A0
  gsub("\u0001", "\u00A0", temp, fixed = TRUE)
}
```

### 修复4：统一连续变量与分类变量的标题行处理（可选）

**背景**：当前逻辑中，分类变量插入独立的标题行（`.__row_order == -1L`），而连续变量直接将其显示名称替换为无缩进的干净变量名。这可能导致在补全缺失组合后，行序出现微妙不一致。

**建议**：为保持一致性，可考虑为连续变量也插入一个轻量级标题行（`.__row_order == -1L`），但**此改动相对较大，可列为可选优化**。

## 实施优先级

### 高优先级（建议立即实施）
1. **修复1**：调整交互作用P值行缩进（2个`\u00A0`）
2. **修复2**：修正亚组差异P值列显示逻辑

### 中优先级（建议测试后实施）
3. **修复3**：增强导出流程的缩进保留

### 低优先级（可选优化）
4. **修复4**：统一连续变量与分类变量的标题行处理

## 测试策略

### 现有测试
- `test_logistic_medical_e2e.R`：基础一致性测试（应继续通过）
- `test_sparse_regression.R`：稀疏数据测试（应继续通过）
- `test_stats_facet.R`：基础功能测试（应继续通过）

### 新增测试建议
创建 `test_regression_indent_consistency.R`，专项测试：
1. 单亚组场景的交互作用P值行缩进
2. 亚组+列分组场景的亚组差异P值列显示
3. 前后端数据提取的缩进一致性
4. 导出文档的缩进保留

## 风险控制

1. **回归风险**：所有改动应确保现有测试用例100%通过。
2. **性能影响**：修复2涉及额外的循环查找，但对性能影响可忽略（变量数通常<20）。
3. **兼容性**：保持与现有前端渲染逻辑（`apply_clinical_style`）的兼容。

## 附录：关键代码位置

### 核心函数
- `build_unified_regression_table`：`modules/common/analysis_shared.R` 第237-746行
- 缩进关键位置：
  - 第325行：为所有项添加4个`\u00A0`缩进
  - 第562行：交互作用P值行创建（需修改）
  - 第545-551行：亚组差异P值列设置（需修改）
  - 第672-722行：亚组缩进与父级标题行插入（保持不变）

### 导出相关
- `extract_table_dataframe`：`modules/common/table_export.R` 第34-85行
- `clean_label`：同文件第57-64行（需检查）
- `build_sci_flextable`：同文件第222-412行
- `clean_gt_label`：同文件第226-235行（需检查）

### 前端样式
- `apply_clinical_style`：`modules/statistical_analysis/linear.R` 第191-212行（保持不变）

## 总结

本优化方案聚焦于**最小化改动**，通过调整缩进层级、修正显示逻辑、增强导出稳定性三个方向，解决前后端不一致与层级缩进失效问题。所有建议均以保持现有表格结构为前提，确保用户熟悉的显示布局不受影响。

实施顺序建议按优先级进行，每完成一项修复即运行完整测试套件，确保无回归问题。

---
*文档生成时间：2026-03-25*  
*基于AutoTFL代码版本分析*
# 统计分析模块改进报告（任务交接指引）

## 一、改进目标
- 统一 Cox / Logistic / Linear 在“亚组差异P值”上的统计口径与展示行为。
- 区分“亚组变量（split）”与“分层变量（模型控制项）”。
- 提升可复现代码可读性（注释化、结构化、参数显式化）。

## 二、已完成改动

### 1) Cox
- 参数文案统一：
  - 行分组改为“亚组变量 (Split, 行分组)”
  - “真实分层控制变量”改为“分层变量 (strata)”
- 统计口径统一：
  - 亚组差异P值定义为“协变量×亚组”的交互项检验P值。
  - 有列分组时，按每个列分组子数据独立计算该P值。
  - 无亚组时，隐藏亚组差异P值列。
- 模型控制：
  - 支持 `strata(分层变量)` 进入 Cox 模型进行基线风险分层控制。
- 解读增强：
  - 在结果解读中明确亚组差异P值统计意义及列分组口径。
- 可复现代码：
  - 增加分段注释（加载包、参数、映射、公式、拟合、输出），结构更清晰。

### 2) Logistic
- 参数文案统一：
  - 行分组改为“亚组变量 (Split, 行分组)”
  - 新增“分层变量 (控制项) - 可选”
- 统计口径统一：
  - 亚组差异P值改为交互项检验P值。
  - 有列分组时按分组子数据独立计算并展示每组独立的差异P值列。
  - 无亚组时不显示差异P值列。
- 解读增强：
  - 增加亚组差异P值定义与列分组说明。
- 可复现代码：
  - 新增 `generate_logistic_code()`，含注释与结构化步骤。

### 3) Linear
- 参数文案统一：
  - 行分组改为“亚组变量 (Split, 行分组)”
  - 新增“分层变量 (控制项) - 可选”
- 统计口径统一：
  - 亚组差异P值改为交互项检验P值。
  - 有列分组时按分组子数据独立计算并展示每组独立的差异P值列。
  - 无亚组时不显示差异P值列。
- 解读增强：
  - 增加亚组差异P值定义与列分组说明。
- 可复现代码：
  - 新增 `generate_linear_code()`，含注释与结构化步骤。

### 4) 主调度模块
- `modules/statistical_analysis.R` 已接入：
  - `logistic_model_strata`
  - `linear_model_strata`
- 分析执行时传入新增参数，保证 UI/Server 贯通。

## 三、当前口径（必须遵循）
- 亚组差异P值 = 交互项检验P值（effect modification），不是亚组均值差。
- 两组亚组时：参考组行通常留空，比较组显示 “比较组 vs 参考组” 的交互P值。
- 多亚组时：以第一个亚组为参考，其余亚组分别显示对应比较P值。
- 列分组场景：每个列分组独立计算，不共用全局P值。

## 四、已完成验证
- Cox / Logistic / Linear 全部完成运行验证：
  - 有亚组 + 有列分组：存在每组独立差异P值列；
  - 无亚组 + 有列分组：差异P值列隐藏；
  - 可复现代码文本可解析。
- 语言诊断检查通过（无新增诊断错误）。

## 五、下一任务窗口建议
- 若继续优化，优先级建议：
  1. 将“参考亚组选择”做成可配置（默认首水平）。
  2. 为三类回归新增“全局交互联合检验”输出区（补充局部对比P值）。
  3. 为可复现代码增加“最小可运行示例数据模板”开关。
  4. 将同口径扩展到其他统计模块（若后续引入亚组交互分析）。

## 六、快速验收清单
- UI 是否出现：
  - Logistic/Linear 的“分层变量 (控制项)”
  - Cox 的“分层变量 (strata)”
- 亚组差异P值是否满足：
  - 无亚组不显示；
  - 有列分组按组分别显示；
  - 比较组显示，参考组空白。
- 结果解读是否明确写出：
  - “交互项检验口径”
  - “列分组子数据独立计算”

# 数据准备模块 - 高级空值筛选功能文档

## 功能概述

在数据准备模块的高级筛选功能中，我们新增了对空值的高级筛选支持。用户现在可以更灵活地处理数据中的空值（NA值），包括：

1. **全部** - 显示所有记录（包括空值和非空值）
2. **排除空值** - 只显示非空值的记录
3. **仅显示空值** - 只显示空值的记录

## 使用方法

### 1. 筛选选项说明

在高级筛选面板中，每个变量现在都有一个"空值筛选"单选按钮组，包含以下选项：

- **全部**：默认选项，不对空值进行筛选
- **排除空值**：只保留该变量非空的记录
- **仅显示空值**：只保留该变量为空的记录

### 2. 不同变量类型的筛选行为

- **数值型变量**：当选择"仅显示空值"时，其他数值范围筛选将被忽略
- **分类变量**：当选择"仅显示空值"时，其他分类值筛选将被忽略
- **日期变量**：当选择"仅显示空值"时，其他日期范围筛选将被忽略
- **文本变量**：当选择"仅显示空值"时，其他文本搜索筛选将被忽略

### 3. 注意事项

- 选择"仅显示空值"时，该变量的其他筛选条件将自动被忽略
- 选择"排除空值"时，该变量的其他筛选条件仍然有效
- 选择"全部"时，该变量的其他筛选条件仍然有效
- 这些筛选条件是针对单个变量的，可以为不同变量设置不同的空值筛选选项

### 4. 重置功能

点击"重置所有筛选"按钮将把所有变量的空值筛选选项重置为"全部"，并重置其他筛选条件。

## 技术实现

### UI变更

- 将原有的"排除空值"复选框替换为"空值筛选"单选按钮组
- 提供"全部"、"排除空值"、"仅显示空值"三个选项

### 逻辑变更

- 更新了筛选逻辑，确保在"仅显示空值"模式下忽略其他筛选条件
- 修复了在应用空值筛选时仍应用其他筛选条件的问题
- 确保重置功能正确重置新的空值筛选选项

## 兼容性

此功能与现有功能完全兼容，不会影响现有的筛选逻辑和用户体验。所有现有功能保持不变，只是增加了新的空值筛选选项。